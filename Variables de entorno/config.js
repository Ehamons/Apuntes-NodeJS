//El archivo config.js es el archivo de configuración en donde realizamos
//nuestra variable de entorno
module.exports = {
  //Variables de entorno
  //IP por donde sale el servidor (Hay múltiples IP de desarrollo estándar, la que se usa es una de ellas)
  host: "127.0.0.1",
  //puerto en donde sale el servidor
  port: 8000,
};
